-- Insert admin user (Note: For Supabase Auth, create this user through the dashboard)
-- This is just sample data - you'll need to create the admin user via Supabase Auth
-- INSERT INTO users (id, email, full_name, is_admin) VALUES
-- ('admin-uuid-here', 'admin@carpolish.com', 'Admin User', TRUE);

-- Note: Products are now static and defined in the application code
-- No product seeding needed
